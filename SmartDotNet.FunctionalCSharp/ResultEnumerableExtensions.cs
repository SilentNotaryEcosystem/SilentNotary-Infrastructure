namespace SmartDotNet.FunctionalCSharp
{
    public static class ResultEnumerableExtensions
    {/*
        public static Result OnAllSuccess<T>(this IEnumerable<Result<T>> results, Action<IEnumerable<T>> action)
        {
            var resultsArray = results.ToArray();
            return resultsArray.OnAllSuccess(action);
        }

        public static Result<IEnumerable<T>> OnAllSuccess<T>(this IEnumerable<Result<T>> results, Func<IEnumerable<T>> func)
        {
            var resultsArray = results.ToArray();
            return resultsArray.OnAllSuccess(func)
        }*/
    }
}
